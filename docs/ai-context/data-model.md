Always update this context file whenever related changes are made.

# Data Model Context

- Shared types and Zod schemas (Book, LookupRequest/Response) in packages/shared/src/schemas.ts.
- Future: align Supabase schema with shared contracts and keep migrations in sync.
